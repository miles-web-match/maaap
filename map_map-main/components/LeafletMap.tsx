import { View, Text } from 'react-native';
export default function LeafletMap() {
  return (
    <View style={{ flex:1, alignItems:'center', justifyContent:'center' }}>
      <Text>（モバイルの地図は後で対応。今はWebのみ表示）</Text>
    </View>
  );
}
